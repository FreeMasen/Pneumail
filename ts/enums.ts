export enum SideBarState {
    Open = 'open',
    Closed = 'closed'
}

export enum IconPosition {
    Right = 'right',
    Left = 'left'
}

export enum DBWorkerState {
    Ready = 'ready',
    NotReady = 'not-ready',
    
}